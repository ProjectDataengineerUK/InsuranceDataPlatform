resources:
  jobs:
    gold_aggregate:
      name: insurance-gold-aggregate-${bundle.target}
      schedule:
        quartz_cron_expression: "0 */10 * * * ?"
        timezone_id: "America/Sao_Paulo"
        pause_status: UNPAUSED

      # Workspace trial só permite compute serverless — environment_key obrigatório.
      # Lê de gold.claims, que é populado continuamente pelo job
      # fraud_score_stream (ver resources/jobs.fraud_score.yml) — este job só
      # recalcula os agregados por região e reaplica a governança.
      tasks:
        - task_key: gold_claims_agg
          environment_key: default
          spark_python_task:
            python_file: ../src/streaming/gold_aggregate.py
            parameters:
              - "--catalog"
              - "${var.catalog}"
              - "--gold-claims-table"
              - "${var.catalog}.gold.claims"
              - "--gold-agg-table"
              - "${var.catalog}.gold.claims_region_agg"
              - "--results-table"
              - "${var.catalog}.monitoring._dq_results"
              - "--run-date"
              - "{{job.start_time.iso_date}}"

        - task_key: sla_monitor
          environment_key: default
          depends_on:
            - task_key: gold_claims_agg
          spark_python_task:
            python_file: ../src/monitoring/sla_alerts.py
            parameters:
              - "--gold-claims-table"
              - "${var.catalog}.gold.claims"
              - "--sla-hours"
              - "24"

      environments:
        - environment_key: default
          spec:
            environment_version: "2"
            dependencies:
              - requests
