# InsuranceDataPlatform

> Projeto ainda não iniciado. O diretório contém apenas um arquivo `context.md` vazio — nenhum código, stack ou estrutura foi definida até o momento.

---

## Stack

- Nenhuma stack detectada ainda (nenhum manifest de dependências, código-fonte ou configuração presente).

## Estrutura

```
.
├── .claude/       # hooks e configuração do agentcode
└── context.md     # vazio
```

## Arquivos-chave

| Arquivo | Função |
|---------|--------|
| `context.md` | Vazio — reservado para contexto do projeto (ainda não preenchido) |

## Convenções

- **Linter:** não configurado
- **Formatter:** não configurado
- **Testes:** não configurado

## Como rodar

Não aplicável — nenhum código ou dependência definida ainda.

---

## Agentes recomendados (agentcode)

| Agente | Quando usar |
|--------|-------------|
| `@brainstorm-agent` | Explorar ideias e escopo antes de definir requisitos |
| `@the-planner` | Planejar arquitetura assim que o escopo estiver mais claro |
| `@design-agent` | Desenhar arquitetura técnica após definição de requisitos |
| `@code-reviewer` | Revisão de código assim que houver implementação |
| `@security-reviewer` | Revisão de segurança assim que houver implementação |

## Comandos úteis

| Comando | Quando usar |
|---------|-------------|
| `/brainstorm` | Explorar a ideia do projeto (ex.: "plataforma de dados de seguros") |
| `/define` | Capturar requisitos após o brainstorm |
| `/design` | Criar arquitetura técnica após definição de requisitos |
| `/status` | Ver status do projeto a qualquer momento |
| `/preflight` | Checar prontidão do projeto |

---

_Gerado por `/start --force` em 2026-07-08. Regenere este arquivo assim que o projeto tiver stack, estrutura e código reais._
